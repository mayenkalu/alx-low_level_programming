#include "main.h"
/**
 * factorial - prints fatorial of a given number.
 * @n - given number.
 * Return: -1 if number is lower than 0;
 * 	else return fatorial of number.
 */
int factorial(int n)
{
	if (n = 0)
		return (1);
	else
		return (n * factorial(n - 1);
	else
		return (-1);
}

